sudo: required
services:
  - docker

notifications:
  email: false

before_install:
  - docker build -t kvaksa/docker-react -f Dockerfile.dev .

script:
  - docker run kvaksa/docker-react npm run test -- --coverage